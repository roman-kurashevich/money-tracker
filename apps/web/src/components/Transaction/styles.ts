import { createStyles } from '@mantine/core';

export const useStyles = createStyles(() => ({
  transactionContainer: {
    width: 400,
    padding: 10,
    borderRadius: 10,
    background: 'white',
  },
  closeIcon: {
    cursor: 'pointer',
  },
}));
